# Phase 26 Test: Adapter Contract Generalization

Phase 26 is complete when:

- adapter contract examples validate unchanged;
- invalid payload schema declarations fail with actionable errors;
- invalid topic placeholders and identity mappings fail with actionable errors;
- acceptance criteria can be linked to assertion ids or scenario evidence;
- docs include a customer/evaluator intake checklist;
- `cargo test --workspace --all-targets` passes.

Full `make verify` remains the release gate before presenting a company
evaluation build.

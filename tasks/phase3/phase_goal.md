# Phase 3 Goal — Device Mocks

## Goal

Add practical building-automation mocks for the required v0.1 demos.

## In Scope

- Modbus TCP register map behavior.
- DALI-like scene behavior.
- Contact I/O input behavior.

## Exit Criteria

- `modbus_floor_heating.yaml` runs.
- `dali_scene_partial_failure.yaml` runs.
- Contact input can trigger ops alerts in later phases.
